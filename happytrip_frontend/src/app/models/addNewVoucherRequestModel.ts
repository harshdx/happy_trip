export interface addNewVoucherRequest {
    voucherStartDate: string
    voucherEndDate: string
    voucherStatus: string
    voucherAmount: string
    voucherCode: string
    minimumAmount: string
    discountPercent: string
  }
  